```markdown
你的任務是根據給定的輸入創建一個情境。請仔細遵循以下步驟：

1. 你將會收到一個領域（domain）、子領域（subdomain）和任務相關的實體列表。這些將用於創建連貫的情境和相關功能。

2. 情境設定在一個用戶和一個智能助手之間的對話。

3. 情境應包含姓名、數字和其他標識符，以明確定義問題空間。

4. 以下是輸入資訊：
<domain>{{domain}}</domain>
<subdomain>{{subdomain}}</subdomain>

5. 生成一個簡短的情境，融合該領域和子領域。情境應為你將創建的用戶查詢和功能建立上下文。將你的情境寫在 <scenario> 標籤內。

請注意：
- 所有情境內容必須使用繁體中文撰寫
- 人名應使用中文名字（例如：王小明、李美玲、陳大偉）
- 公司名稱、地點等也應使用中文
- 數字和特定標識符可保留原始格式

現在請生成 {{num_scenarios}} 個不同的情境，每個情境都用 <scenario> 標籤包裹。
```
